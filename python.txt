========================================
PYTHON CTF CHEATSHEET
========================================

[RUN PYTHON]
python3 script.py

========================================
📥 INPUT / OUTPUT
========================================

# input
x = input()

# print
print(x)

========================================
🔁 LOOPS
========================================

for i in range(10):
    print(i)

for c in "hello":
    print(c)

========================================
📜 STRINGS
========================================

s = "hello"

s.lower()
s.upper()
s.strip()
s[::-1]   # reverse

"flag" in s   # check substring

========================================
🔢 CONVERSIONS
========================================

int("123")
str(123)

========================================
🔤 ASCII
========================================

ord('A')   # 65
chr(65)    # 'A'

========================================
🔐 BASE64
========================================

import base64

decoded = base64.b64decode("SGVsbG8=")
print(decoded)

========================================
🔢 HEX
========================================

bytes.fromhex("68656c6c6f")

========================================
🔁 ROT13
========================================

import codecs
print(codecs.decode("uryyb", "rot_13"))

========================================
📡 REQUESTS (WEB)
========================================

import requests

r = requests.get("http://example.com")
print(r.text)

========================================
📂 FILE HANDLING
========================================

# read
with open("file.txt") as f:
    data = f.read()

# write
with open("out.txt", "w") as f:
    f.write("hello")

========================================
🔍 FIND FLAG
========================================

if "flag" in data:
    print(data)

========================================
🧪 BRUTE FORCE
========================================

for i in range(10000):
    print(i)

========================================
📦 SPLIT / JOIN
========================================

s.split(" ")
"-".join(["a","b","c"])

========================================
🔁 REPLACE
========================================

s.replace("a", "b")

========================================
🔢 BINARY
========================================

int("1010", 2)
bin(10)

========================================
🔐 URL DECODE
========================================

import urllib.parse
urllib.parse.unquote("%20")

========================================
🧠 QUICK SCRIPTS
========================================

# reverse string
print(input()[::-1])

# read + search flag
print([l for l in open("file.txt") if "flag" in l])

========================================
🔥 QUICK CHECKLIST
========================================

[ ] Need decode? → base64 / hex
[ ] Need brute? → for loop
[ ] Need file read?
[ ] Need reverse?
[ ] Need search?

========================================
RULES
========================================

- KEEP IT SIMPLE
- WRITE SMALL SCRIPTS
- DON'T OVERCODE
- GOOGLE NOT NEEDED

========================================